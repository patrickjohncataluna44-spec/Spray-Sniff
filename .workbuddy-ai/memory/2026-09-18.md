# 2026-09-18

- Reviewed delivery tracking for an admin-only delivery plan; no application code changed. The customer /track page always enables a mock stage simulator; /api/mock/trackings generates responses without persistence. Real delivery updates use store-engine updateOrderStatus (ADMIN/STAFF) and confirmOwnDelivery (customer receipt sets Delivered).
- Proposed removing customer demo controls, enforcing ADMIN-only delivery changes server-side including alternate mutation paths, and connecting customer tracking to saved delivery history. Existing order persistence can be reused; full courier-stage tracking requires explicit persisted shipment data. Keep payment/cancellation behavior separate. Await user approval before implementation.
